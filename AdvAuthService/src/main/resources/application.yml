server:
  port: 8081

spring:
  application:
    name: advAuthService

  datasource:
    url: jdbc:mysql://localhost:3306/adv_auth_service
    username: root
    password: sourav
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
