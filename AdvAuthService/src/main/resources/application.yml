server:
  port: 8081

spring:
  application:
    name: advAuthService

  datasource:
    url: jdbc:mysql://localhost:3306/adv_auth_service
    username: root
    password: sourav
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  security:
    secret: d75922fedd2165d8c5c7a18178e7362031e29c3baecd33689d8b03c18f7dac75
    access-token-expiration: 3600000
    refresh-token-expiration: 60

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
